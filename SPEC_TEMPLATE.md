# Спецификация фичи: <НАЗВАНИЕ ФИЧИ>

> Шаблон для описания новой фичи или модуля. Копируем в `docs/features/<feature-name>.md`, заполняем все разделы. Разделы в курсиве — подсказки, их удаляем при заполнении.

---

## Описание

*Одно-два предложения: что делает фича, для кого, зачем.*
*Пример: "Позволяет пользователю сохранять пресеты тона (analytical / casual / punchy) и применять их ко всем своим эпизодам автоматически. Для подкастеров с 5+ эпизодами, чтобы не выбирать tone каждый раз."*

## User Stories

*Минимум 3 истории: основной сценарий + 1-2 edge case'а. Формат: "Как <роль>, я хочу <действие>, чтобы <результат>".*

- **US-X.1**: Как <роль>, я хочу <действие>, чтобы <результат>
- **US-X.2**: ...
- **US-X.3**: ...

## Модель данных

*Если нужны новые таблицы или поля в существующих. Типы — как в SQL: uuid, varchar(N), text, jsonb, timestamptz, bigint. Constraints, FK, индексы.*

Таблица `<name>`:

| Поле | Тип | Constraints |
|---|---|---|
| `id` | uuid | PK, default uuid4 |
| ... | ... | ... |
| `created_at` | timestamptz | NOT NULL, default NOW() |
| `updated_at` | timestamptz | NOT NULL |

Индексы:
- `(...)` — для чего используется

*Если меняется существующая модель — явно описать миграцию:*
```
ALTER TABLE artifacts ADD COLUMN tone_preset varchar(32) NULL;
```

## API / внутренние контракты

*Описание новых endpoints или изменений существующих. Формат: метод + путь + тело + success response + все коды ошибок.*

#### `POST /api/...`
Body:
```json
{"field": "value"}
```

Success 200:
```json
{"result": "..."}
```

Errors:
- `400 INVALID_FIELD` — описание условия
- `404 NOT_FOUND` — описание

*Если это внутренний контракт (Celery task / service function) — опиши сигнатуру:*
```python
services.tone_presets.apply(user_id: str, preset_name: str) -> TonePreset
```

## UI / компоненты

*Если есть frontend-часть. Перечислить новые/изменённые компоненты + состояния (loading / error / empty / success). Если UI нет — написать "UI не требуется".*

- `<NewComponent />` — что отображает
  - Состояния: idle, loading, error, success
  - Действия: onClick, onSubmit

## Бизнес-логика

*Правила валидации, формулы, условия переходов между состояниями. Конкретно: "tone_preset должен быть одним из ['analytical', 'casual', 'punchy', 'professional']" — не "валидный tone".*

- Правило 1
- Правило 2

## Крайние случаи

*Таблица: конкретный сценарий → ожидаемое поведение. Минимум 4-5 строк — это самая ценная часть спеки, в ней обычно живут будущие баги.*

| Случай | Ожидаемое поведение |
|---|---|
| Пользователь не заполнил preset и жмёт submit | 400 с code `PRESET_NAME_REQUIRED` |
| Preset с таким именем уже существует у пользователя | 409 `PRESET_ALREADY_EXISTS` |
| ... | ... |

## Dependencies & Contracts

*От каких модулей зависит эта фича. Какие контракты предоставляет другим. Что в SPEC.md ломается при её добавлении (обычно ничего, но проверь).*

**Потребляет**:
- SPEC §<X> — читает <что>
- Модель `<Name>` — поля `<a>`, `<b>`

**Предоставляет**:
- Новый endpoint `POST /api/...` для frontend
- Новое поле `Artifact.tone_preset` для `packager.py`

**Триггерит**:
- Celery task `...` после <условия>

**Не трогает**:
- SPEC §5 Video Clips — контракт не меняется

## Приоритет

| Вопрос | Ответ |
|---|---|
| Нужно для MVP? | да / нет |
| Блокирует другие фичи? | <какие> |
| Блокируется чем-то? | <чем> |
| Оценка времени | <часы> одного разработчика |
| Кому делать | Person A / Person B / совместно |

## Out of scope

*Что эта фича ЯВНО не покрывает, хотя могло бы показаться что покрывает. Защита от scope creep'а.*

- НЕ добавляет синхронизацию presets между аккаунтами
- НЕ позволяет кастомные tone (только предопределённые 4)

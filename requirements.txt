# === Core framework ===
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1

# === Async tasks ===
celery==5.4.0
redis==5.0.4

# === DB ===
psycopg[binary]==3.1.19

# === Media processing ===
# ffmpeg вызывается как CLI; pydub — для простых операций с аудио
pydub==0.25.1
# Pillow для quote-графики и thumbnails
Pillow==10.3.0

# === External APIs ===
# openai 1.30.x crashes at client init under httpx>=0.28 (`proxies` kwarg
# was removed). 1.55+ adopts the new httpx API.
openai==1.55.3
anthropic==0.28.0

# === URL ingestion ===
yt-dlp==2024.5.27

# === Utilities ===
python-dotenv==1.0.1
pydantic==2.7.1
requests==2.32.2

# === HTML → PNG для quote-графики (альтернатива Pillow-версткe) ===
playwright==1.44.0

# === Dev ===
pytest==8.2.1
pytest-django==4.8.0
black==24.4.2
ruff==0.4.4
